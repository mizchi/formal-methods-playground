name: lean

on:
  push:
  pull_request:
    paths:
      - ".github/workflows/lean.yml"
      - "flake.lock"
      - "flake.nix"
      - "justfile"
      - "languages/lean/**"
      - "lean-toolchain"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  lean:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check Lean probes
        run: nix develop -c just check-lean
