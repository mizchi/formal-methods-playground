SPECIFICATION Spec
CONSTANT Items = {i1, i2}
INVARIANT TypeOK
INVARIANT NoRefundWithoutPaid
PROPERTY PaymentResolves
\* Terminal states (cancelled / refunded / shipped) are by design
\* — TLC treats them as deadlock without this hint.
CHECK_DEADLOCK FALSE
