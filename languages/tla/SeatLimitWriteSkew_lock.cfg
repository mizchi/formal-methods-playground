\* The fix teams usually ship: keep SI, but SELECT ... FOR UPDATE the org row
\* so the two requests serialise on an explicit lock.
SPECIFICATION Spec
CONSTANTS
  Txns = {t1, t2}
  SeatLimit = 1
  Isolation = "SI_LOCK"
INVARIANT TypeOK
INVARIANT SeatsWithinLimit
CHECK_DEADLOCK FALSE
