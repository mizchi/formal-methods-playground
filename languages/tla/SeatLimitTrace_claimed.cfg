\* Breaking variant 1 of 2 -- run by hand. The INC-2291 log replayed at the
\* level ops maintain the pool is set to. SSI would have aborted t2's COMMIT,
\* and the log says it succeeded, so the model refuses that line.
\* Running this SHOULD report TraceAccepted violated, with i = 6 in the final
\* state: log line 6, t2's COMMIT.
\* Domain reading: this history cannot have happened under SERIALIZABLE.
SPECIFICATION Spec
CONSTANTS
  Txns = {"t1", "t2"}
  SeatLimit = 1
  Isolation = "SERIALIZABLE"
  Trace <- IncidentLog
INVARIANT TypeOK
INVARIANT SeatsWithinLimit
PROPERTY TraceAccepted
CHECK_DEADLOCK FALSE
