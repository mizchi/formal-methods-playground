\* Breaking variant: split read+record -> read-modify-write race -> over-grant.
\* Running this SHOULD report NoOverGrant violated (granted = Cap+1).
SPECIFICATION Spec
CONSTANTS
  Workers = {w1, w2}
  Cap = 1
  Atomic = FALSE
INVARIANT TypeOK
INVARIANT NoOverGrant
CHECK_DEADLOCK FALSE
