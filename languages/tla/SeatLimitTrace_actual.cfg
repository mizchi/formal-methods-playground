\* Breaking variant 2 of 2 -- run by hand. The SAME INC-2291 log, replayed at
\* the level that does admit it. Now every line conforms and the failure moves:
\* running this SHOULD report SeatsWithinLimit violated with members = 2 on a
\* one-seat plan.
\* Domain reading: the pool was on REPEATABLE READ, and the seat limit really
\* did break. Pairing this with SeatLimitTrace_claimed.cfg is what turns "the
\* org has an extra member" into "the isolation level is not what we think".
SPECIFICATION Spec
CONSTANTS
  Txns = {"t1", "t2"}
  SeatLimit = 1
  Isolation = "SI"
  Trace <- IncidentLog
INVARIANT TypeOK
INVARIANT SeatsWithinLimit
PROPERTY TraceAccepted
CHECK_DEADLOCK FALSE
