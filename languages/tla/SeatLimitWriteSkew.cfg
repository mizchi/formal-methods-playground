\* PostgreSQL SERIALIZABLE (SSI): the commit-time read-set check aborts the
\* second transaction, so the seat limit holds. CI-green check.
SPECIFICATION Spec
CONSTANTS
  Txns = {t1, t2}
  SeatLimit = 1
  Isolation = "SERIALIZABLE"
INVARIANT TypeOK
INVARIANT SeatsWithinLimit
CHECK_DEADLOCK FALSE
